# config.py

class Config:
    DEBUG = True
    HOST = "0.0.0.0"
    PORT = 5000
    # MODEL_PATH = "../model/t5_finetuned_pubmed"  # Path to the saved T5 model
    # MODEL_PATH = "../model/special_tokens"  # Path to the saved T5 model
    MODEL_PATH = "../model/checkpoint-330"  # Path to the saved T5 model